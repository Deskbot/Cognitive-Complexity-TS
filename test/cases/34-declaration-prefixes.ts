export function exportFunctionDeclaration() {
    while (true);
}

export const exportConstAnonymousFunction = () => {
    while (true);
};

export let exportLetAnonymousFunction = () => {
    while (true);
};

async function asyncDeclaration() {
    while (true);
}

const asyncAnonymous = async () => {
    while (true);
};

export async function exportAsyncDeclaration() {
    while (true);
}

export const exportAsyncAnonymous = async () => {
    while (true);
};

export namespace ExportNamespace {

}

export class ExportClass {

}
